{
  "name": "@donmoa/database",
  "version": "1.0.0",
  "private": true,
  "description": "Database migrations and schema",
  "scripts": {
    "migrate": "echo 'Run migrations via Supabase CLI'",
    "seed": "echo 'Run seed data'"
  }
}
